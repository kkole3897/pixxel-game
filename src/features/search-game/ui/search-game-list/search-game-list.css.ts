import { style } from '@vanilla-extract/css';

export const searchGameList = style({
  display: 'flex',
  flexDirection: 'column',
  rowGap: '24px',
  padding: '16px',
});

export const searchGameListItem = style({
  width: '100%',
});
