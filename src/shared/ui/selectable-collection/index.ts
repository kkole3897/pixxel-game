import { default as SelectableCollection } from './selectable-collection';
import { default as SelectableCollectionItem } from './selectable-collection-item';

const Root = SelectableCollection;
const Item = SelectableCollectionItem;

export { SelectableCollection, SelectableCollectionItem, Root, Item };
